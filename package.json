{
  "name": "opencode-delegated-access",
  "version": "0.5.0",
  "description": "OpenCode plugin that auto-approves safe requests via an LLM safety classifier and keeps risky ones visible for human review.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "permissions",
    "safety",
    "auto-approve"
  ],
  "license": "MIT",
  "dependencies": {
    "@opencode-ai/plugin": "1.18.5",
    "@opencode-ai/sdk": "1.18.5",
    "node-notifier": "^10.0.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/node-notifier": "^8.0.5",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "opencode": ">=1.18.5"
  }
}
